# freewha
```sh
# tinggal copy paste kode perintah di bawah
```
```sh
apt-get update && apt-get upgrade -y && apt-get install wget
wget https://github.com/JaeXploit/freewha/blob/main/run.py
apt-get install python
pip install requests
pip install bs4
python run.py
```
